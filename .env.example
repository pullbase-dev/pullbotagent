# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/pullbase

# OpenAI API key — used for summarize, review, tags, compare, and free-form Q&A
OPENAI_API_KEY=sk-...

# (Optional) Override the OpenAI model used. Defaults to gpt-4o-mini.
# OPENAI_MODEL=gpt-4o-mini
